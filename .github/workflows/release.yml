name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7

      - name: Build + package
        run: ./scripts/build.sh --zip

      - name: Checksum
        run: |
          cd build
          shasum -a 256 Pookify-Copilot.zip > Pookify-Copilot.zip.sha256
          cat Pookify-Copilot.zip.sha256

      - name: Publish release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            build/Pookify-Copilot.zip
            build/Pookify-Copilot.zip.sha256
          generate_release_notes: true
          body: |
            A locally built app isn't quarantined, but a downloaded zip is. After downloading, run:

                xattr -dr com.apple.quarantine "/Applications/Pookify Copilot.app"

            See the README for install details. Verify the download with:

                shasum -a 256 -c Pookify-Copilot.zip.sha256
